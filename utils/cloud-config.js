/**
 * Cloud environment configuration for the mini program.
 * Replace `YOUR_CLOUD_ENV_ID` with the environment ID from the WeChat cloud console.
 */
const cloudEnvId = 'YOUR_CLOUD_ENV_ID'

module.exports = {
  cloudEnvId
}
